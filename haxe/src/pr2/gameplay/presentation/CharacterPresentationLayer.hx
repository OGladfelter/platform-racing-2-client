package pr2.gameplay.presentation;

/**
	Display-list side of the level art used by a character presentation pose.
**/
enum abstract CharacterPresentationLayer(Int) {
	var Front = 0;
	var Back = 1;
}
